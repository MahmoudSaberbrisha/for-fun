<!DOCTYPE html>
<html lang="<%= lang %>" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <title><%= t('opinionPoll.title') || 'Opinion Poll' %></title>
    <link rel="stylesheet" href="/frontend/styles/style.css" />
    <link rel="stylesheet" href="/frontend/styles/custom-sidebar.css" />
    <style>
      body {
        margin-top: -10px;
        padding: 0;
        min-height: 100vh;
        width: 100vw;
        background: linear-gradient(135deg, #e8f5e9 0%, #ffffff 100%);
        font-family: "Segoe UI", Arial, sans-serif;
      }
      .container {
        width: 100vw;
        min-height: 100vh;
        display: flex;
        flex-direction: row;
        align-items: stretch;
        justify-content: flex-start;
        box-sizing: border-box;
      }
      .main-content {
        flex: 1;
        padding: 48px 32px;
        display: flex;
        flex-direction: column;
        align-items: center;
        background: rgba(255, 255, 255, 0.95);
      }
      h1 {
        color: #218c4c;
        margin-bottom: 32px;
        font-size: 2.5rem;
        letter-spacing: 1px;
      }
      form {
        background: #f1f8f4;
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(33, 140, 76, 0.08);
        padding: 36px 32px 28px 32px;
        width: 100%;
        max-width: 480px;
        display: flex;
        flex-direction: column;
        gap: 18px;
      }
      label {
        color: #218c4c;
        font-weight: 600;
        margin-bottom: 6px;
        font-size: 1.08rem;
      }
      input[type="text"],
      select {
        padding: 10px 12px;
        border: 1.5px solid #b2dfdb;
        border-radius: 8px;
        font-size: 1rem;
        background: #fff;
        transition: border 0.2s;
        outline: none;
      }
      input[type="text"]:focus,
      select:focus {
        border-color: #218c4c;
      }
      button[type="submit"] {
        margin-top: 12px;
        background: linear-gradient(90deg, #43a047 60%, #66bb6a 100%);
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 12px 0;
        font-size: 1.1rem;
        font-weight: bold;
        cursor: pointer;
        box-shadow: 0 2px 8px rgba(33, 140, 76, 0.1);
        transition: background 0.2s, transform 0.1s;
      }
      button[type="submit"]:hover {
        background: linear-gradient(90deg, #388e3c 60%, #43a047 100%);
        transform: translateY(-2px) scale(1.03);
      }
      @media (max-width: 900px) {
        .container {
          flex-direction: column;
        }
        .main-content {
          padding: 24px 8px;
        }
        form {
          padding: 20px 8px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="container" style="margin-top: 40px">
      <%- include('partials/sidebar') %>
      <div class="main-content">
        <h1><%= t('opinionPoll.header') || 'Create Opinion Poll' %></h1>
        <form action="/api/opinionPolls/create" method="POST">
          <label for="question"
            ><%= t('opinionPoll.questionLabel') || 'Question:' %></label
          >
          <input
            type="text"
            id="question"
            name="question"
            required
            minlength="2"
          />
          <label for="option1"
            ><%= t('opinionPoll.option1Label') || 'Option 1:' %></label
          >
          <input
            type="text"
            id="option1"
            name="option1"
            required
            minlength="2"
          />
          <label for="option2"
            ><%= t('opinionPoll.option2Label') || 'Option 2:' %></label
          >
          <input
            type="text"
            id="option2"
            name="option2"
            required
            minlength="2"
          />
          <label for="status"
            ><%= t('opinionPoll.statusLabel') || 'Status:' %></label
          >
          <select id="status" name="status">
            <option value="accepted">
              <%= t('opinionPoll.statusAccepted') || 'Accepted' %>
            </option>
            <option value="rejected">
              <%= t('opinionPoll.statusRejected') || 'Rejected' %>
            </option>
            <option value="pending" selected>
              <%= t('opinionPoll.statusPending') || 'Pending' %>
            </option>
          </select>
          <button type="submit">
            <%= t('opinionPoll.submitButton') || 'Create Poll' %>
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
